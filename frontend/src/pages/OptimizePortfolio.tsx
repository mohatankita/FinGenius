import { Box, Typography } from '@mui/material';

const OptimizePortfolio = () => {
  return (
    <Box p={3}>
      <Typography variant="h4" gutterBottom>
        Optimize Portfolio
      </Typography>
      {/* Add summary cards, pie chart, etc. */}
    </Box>
  );
};

export default OptimizePortfolio;
